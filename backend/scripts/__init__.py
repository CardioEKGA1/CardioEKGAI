# Marker file so backend.scripts is importable as a package, which lets
# database.py auto-seed the Yogananda messages on boot via:
#     from scripts.seed_yogananda_messages import seed_into_session
